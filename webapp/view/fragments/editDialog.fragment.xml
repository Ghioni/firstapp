<c:FragmentDefinition
	xmlns="sap.m"
	xmlns:c="sap.ui.core"
    xmlns:l="sap.ui.layout"
	xmlns:f="sap.ui.layout.form"
	xmlns:mvc="sap.ui.core.mvc"
>
<Dialog
      id="editDialog"
      title="Insert"
      >
        <f:SimpleForm 
        layout="ResponsiveGridLayout"
        >
        <f:toolbar>
            <Toolbar >
                <Title text="Form"/>
            </Toolbar>
        </f:toolbar>
        <f:content>
            <Label text="ID Fornitore" />
            <Input value="{oForm>/SupplierID}" editable="false"/>
            <Label text="Nome Compagnia" />
            <Input value="{oForm>/CompanyName}"/>
            <Label text="Nome Contatto" />
            <Input value="{oForm>/ContactName}" />
            <Label text="Titolo" />
            <Input value="{oForm>/ContactTitle}" />
            <Label text="Indirizzo" />
            <Input value="{oForm>/Address}" />
            <Label text="Città" />
            <Input value="{oForm>/City}" />
            <Label text="Regione" />
            <Input value="{oForm>/Region}" />
            <Label text="Codice Postale" />
            <Input value="{oForm>/PostalCode}" />
            <Label text="Paese" />
            <Input value="{oForm>/Country}" />
            <Label text="Telefono" />
            <Input value="{oForm>/Phone}" />
            <Label text="Fax" />
            <Input value="{oForm>/Fax}" />

            <Button text="Edit" press="onPressChange"/>
            <Button text="Close" press="close"/>
        </f:content>
    </f:SimpleForm>
      
   </Dialog>
</c:FragmentDefinition>