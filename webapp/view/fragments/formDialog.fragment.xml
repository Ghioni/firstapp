<c:FragmentDefinition
	xmlns="sap.m"
	xmlns:c="sap.ui.core"
    xmlns:l="sap.ui.layout"
	xmlns:f="sap.ui.layout.form"
	xmlns:mvc="sap.ui.core.mvc"
>
<Dialog
      id="helloDialog"
      title="Insert"
      >
        <f:SimpleForm 
        layout="ResponsiveGridLayout"
        >
        <f:toolbar>
            <Toolbar>
                <Title text="Form"/>
            </Toolbar>
        </f:toolbar>
        <f:content>
            <Label text="ID Fornitore" />
            <Input value="{FormModel>/SupplierID}" />
            <Label text="Nome Compagnia" />
            <Input value="{FormModel>/CompanyName}"/>
            <Label text="Nome Contatto" />
            <Input value="{FormModel>/ContactName}" />
            <Label text="Titolo" />
            <Input value="{FormModel>/ContactTitle}" />
            <Label text="Indirizzo" />
            <Input value="{FormModel>/Address}" />
            <Label text="Città" />
            <Input value="{FormModel>/City}" />
            <Label text="Regione" />
            <Input value="{FormModel>/Region}" />
            <Label text="Codice Postale" />
            <Input value="{FormModel>/PostalCode}" />
            <Label text="Paese" />
            <Input value="{FormModel>/Country}" />
            <Label text="Telefono" />
            <Input value="{FormModel>/Phone}" />
            <Label text="Fax" />
            <Input value="{FormModel>/Fax}" />

            <Button text="Add" press="addOnPress"/>
            <Button text="Close" press="closeOnPress"/>
        </f:content>
    </f:SimpleForm>
      
   </Dialog>
</c:FragmentDefinition>